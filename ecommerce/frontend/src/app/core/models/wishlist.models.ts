export interface WishlistItem {
  id: number;
  productId: number;
  productName: string;
  productPrice: number;
  productImageUrl: string;
  productCategory: string;
  productStock: number;
  addedAt: string;
}
